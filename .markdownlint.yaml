# Relaxed rules — this project's docs use long prose lines, inline HTML badges
# and emoji headings on purpose.
default: true

MD013: false   # line length
MD033: false   # inline HTML (README badges)
MD041: false   # first line does not have to be a top-level heading
MD024:
  siblings_only: true   # CHANGELOG repeats "### Added" under each version
